name: real_quest
description: Real Quest - RPG x Real World Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  supabase_flutter: ^2.10.0
  flutter_riverpod: ^2.3.6
  go_router: ^10.0.0
  google_fonts: ^5.1.0
  geolocator: ^10.0.0
  camera: ^0.10.5
  mobile_scanner: ^5.2.3
  cached_network_image: ^3.2.3
  flutter_animate: ^4.2.0
  percent_indicator: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
